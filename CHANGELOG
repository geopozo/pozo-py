v0.9.25:
 - TreeTable() renderer now better organized and with options
 - cpus=1 on makemovie now turns off parallel library
v0.9.24:
 - Add trial for fill options
 - Fix some bugs
v0.9.23:
 - Refactor annotation systems
 - Add per-track annotations
 - depth_lock set to false for xp renderer
v0.9.22:
 - fix to CI system
v0.9.21:
 - fix to CI system
v0.9.20:
 - add a "summarize" function for printing violin/box plots
 - HasLog() now properly returns all items that HasLog()
 - replace_tracks will add tracks OR replace by mnemonic
 - combine_tracks will ignore tracks already combined
 - replace_traces is like replace_tracks but focuses on Trace
 - get_trace()/axis()/track() now defaults to selector=0
 - axis if they're missing a range/unit/scale type property look @ their children for first match
v0.9.19:
 - Ood now includes exclude to filter out selectors
 - There is a declare option for using a dictionary to describe graphs from las files
v0.9.18:
 - We now can make movies of depth ranges with crossplots
 - We export to las files and curve items
 - We add some units from other las files
 - We also now treat "string" as HasLog() instead of Name
v0.9.17:
 - Add license (non-commerical use)
 - Update deps
v0.9.16:
 - Fix bug with unit override
 - No longer support style initialized in Graph()
 - Add ability to put separate depth_range on render
 - Add ability to specify color range on render
 - Can now specify depth for xp by index
 - Add function to produce videos!
 - Add a reverse unit map to pozo.units.registry
 - Specify whole graph annotations by depth!
v0.9.15:
 - Documentation improvements
 - Enhanced information about units in check_las
 - Units override when establish `Graph` w/ unit_map={}
v0.9.14:
 - BIG BREAKING CHANGE: change pozo.Data to pozo.Trace
v0.9.13:
 - flesh out crossplot api
v0.9.10:
 - add crossplot function to plotly renderer
 - get_data and get_depth now take an optional second argument to slice_by_depth
v0.9.9:
 - support welly objects in initialization
v0.9.5:
 - fix typo bug in combine_graphs()
v0.9.4:
 - fix typo bug in combine_graphs()
v0.9.3:
 - combine_tracks() now allows you to use new tracks and functions like add_tracks
   (only the first argument can be a Data or Axis as well)
v0.9.2:
 - Allow depth_unit to be None w/o Error
 - add_tracks/axes/data now return the items finally added
v0.9.1:
 - Add README.md
 - Change `depth` to `show_depth` and `depth_range` to `depth` in interface
